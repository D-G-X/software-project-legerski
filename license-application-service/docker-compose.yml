services:
  postgres:
    image: postgres:18.0
    environment:
    - POSTGRES_DB=license-application-service
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=P4ssword!
    ports:
    - 5433:5432
